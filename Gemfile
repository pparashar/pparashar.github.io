source "https://rubygems.org"
gemspec

# Needed for local `jekyll build`/`serve` with `markdown: kramdown` + `input: GFM`
# (kramdown 2.x split the GFM parser into its own gem). GitHub Pages provides
# this via its own gem set, so it does not affect the deployed build.
gem "kramdown-parser-gfm"